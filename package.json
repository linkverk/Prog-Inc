{
  "name": "zero-to-ten-x",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "gen": "node scripts/gen-content.mjs",
    "stats": "node scripts/content-stats.mjs",
    "typecheck": "tsc --noEmit",
    "build": "npm run gen && npm run typecheck && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
